{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "dev": "./node_modules/nodemon/bin/nodemon.js --watch src --exec ts-node src/main.ts"
  },
  "devDependencies": {
    "@types/node": "^22.4.1",
    "nodemon": "^3.1.4"
  },
  "dependencies": {
    "@xhayper/discord-rpc": "^1.2.0"
  }
}
